export interface DirectChatIndex {
    ownId: string, //Own user id
    partnerId: string, //User ID of chat partner
    lastTimeStamp: any, //last update of the chat
    directChatId: string, // ID of the Chat.
}
